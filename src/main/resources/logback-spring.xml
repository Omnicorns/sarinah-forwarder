<configuration>

    <!-- 1) Appender: semua log ke console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 2) Appender: hanya API logs ke file -->
    <appender name="API_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/api.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/api.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>7</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 3) Logger khusus ApiLoggingFilter -->
    <logger name="com.sarinah.sales.configuration.ApiLoggingFilter"
            level="INFO" additivity="false">
        <!-- Hanya kirim ke API_FILE, tidak ke console lewat additivity -->
        <appender-ref ref="API_FILE"/>
    </logger>

    <!-- 4) Root logger: semua log (INFO+) tetap ke console -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>